<!-- home page -->
<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/img/hero-image.png" height="510" width="607" class="d-block mx-lg-auto img-fluid" loading="lazy"
            alt="Travelling Blog">
    </div>


    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Huge Collection of adventures Travelling experiences</h1>
        <p class="lead">
            At our travel blog, we ignite the spark of wanderlust within you. With each captivating tale, vivid
            photograph, and valuable tip, we transport you to breathtaking destinations around the globe. From serene
            beaches to bustling city streets, from hidden gems to renowned landmarks, we curate a collection of
            unforgettable experiences.
        </p>

        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>
    </div>
</div>

<!-- Catogories Start -->

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">


    <% if(categories !='' ){ %>
        <% categories.forEach(function(category, index){ %>
            <a href="/categories/<%- category.name %>" class="col text-center category__link ">
                <div class="category__img shadow">
                    <img src="/img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
            <% }) %>
                <% } %>

                    <a href="/categories" class="col text-center category__link ">
                        <div class="category__img shadow">
                            <img src="/img/view-all.jpg" alt="View All" loading="lazy">
                        </div>
                        <div class="pt-1">View All</div>
                    </a>

</div>

<!-- Catogories End -->

<!-- Latest Start -->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if(travelBlogs !='' ) { %>
            <% travelBlogs.latest.forEach(function(blog , index){ %>
                <a href="/blogs/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="/uploads/<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } %>

    </div>
</section>

<!-- Latest End -->


<!-- Indian Blogs Start -->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Indian Blogs</h2>
        <a href="/catagories/Indian" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if(travelBlogs !='' ) { %>
            <% travelBlogs.indian.forEach(function(blog , index){ %>
                <a href="/blogs/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="/uploads/<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found.</p>
        <% } %>

    </div>
</section>

<!-- Indian Blogs End -->


<!-- Japanese Blogs Start -->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Japanese Blogs</h2>
        <a href="/catagories/Japanese" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if(travelBlogs !='' ) { %>
            <% travelBlogs.japanese.forEach(function(blog , index){ %>
                <a href="/blogs/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="/uploads/<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found.</p>
        <% } %>

    </div>
</section>

<!-- Japanese Blogs End -->


<!-- Italian Blogs Start -->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Italian Blogs</h2>
        <a href="/catagories/Italy" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if(travelBlogs !='' ) { %>
            <% travelBlogs.italian.forEach(function(blog , index){ %>
                <a href="/blogs/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="/uploads/<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found.</p>
        <% } %>

    </div>
</section>

<!-- Italian Blogs End -->

















<!-- Submit start -->

<section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish-recipe.png" class="d-block mx-auto mb-4 img-fluid" alt="Pulish your recipe for FREE today"
        width="566" height="208" loading="lazy">
    <h1 class="display-5 fw-bold">Publish your blog for FREE today</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your Blog in front of thousands of people for free.</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-blog" class="btn btn-primary btn-dark btn-lg">Submit Blog</a>
        </div>
    </div>
</section>

<!-- Submit end -->